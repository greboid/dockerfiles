FROM {{image "golang"}} AS build
{{template "clone" (map "repo" "greboid/dockercleanup")}}
{{template "build-go" (map)}}
{{template "go-licenses" (map)}}
{{template "prepare" (map "name" "dockercleanup")}}
{{template "runtime" (map "name" "dockercleanup" "exposes" (arr "8080"))}}
