{{- $repo := "greboid/githubmirror" -}}
{{- $binary := "dotege" -}}
FROM {{image "golang"}} AS build
{{ checkout_tag $repo }}
{{ go_build $repo $binary "" (list) (list) (list) }}
{{ from_base_add_binary $binary (list) (list) }}
