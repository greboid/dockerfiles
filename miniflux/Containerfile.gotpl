{{ set "repo" "miniflux/v2" -}}
{{- set "binary" "miniflux" -}}
{{- set "buildvars" (list (printf "miniflux.app/version.Version=$TAG%s" (tagsafe (get "binary"))) (printf "miniflux.app/version.COMMIT=$TAG%s" (tagsafe (get "binary")))) -}}

FROM {{image "golang"}} AS build
{{ partial "checkout-tag.gotpl" }}
{{ partial "static-go-build.gotpl" }}
{{ partial "from-base-add-binary.gotpl" }}
