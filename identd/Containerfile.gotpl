FROM {{image "golang"}} AS build
{{template "clone" (map "repo" "greboid/identd")}}
ENV CGO_ENABLED=0
{{template "build-go" (map)}}
{{template "go-licenses" (map)}}
{{template "prepare" (map "name" "identd")}}
{{template "runtime" (map "name" "identd" "exposes" (arr "8000"))}}
