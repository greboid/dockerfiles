on:
  workflow_dispatch:
  schedule:
    - cron: '42 12 * * *'
  workflow_call:
name: Update Containers
concurrency: dockerfiles
enable-openid-connect: true
jobs:
  alpine:
    name: Build alpine
    runs-on: alpine
    env:
      PROJECT_NAME: alpine
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  baseroot:
    name: Build baseroot
    needs:
        - alpine
    runs-on: alpine
    env:
      PROJECT_NAME: baseroot
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  git:
    name: Build git
    needs:
        - alpine
    runs-on: alpine
    env:
      PROJECT_NAME: git
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  golang:
    name: Build golang
    needs:
        - alpine
    runs-on: alpine
    env:
      PROJECT_NAME: golang
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  rust:
    name: Build rust
    needs:
        - alpine
    runs-on: alpine
    env:
      PROJECT_NAME: rust
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  alpine-runner:
    name: Build alpine-runner
    needs:
        - alpine
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: alpine-runner
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  base:
    name: Build base
    needs:
        - baseroot
    runs-on: alpine
    env:
      PROJECT_NAME: base
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  centauri:
    name: Build centauri
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: centauri
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  centauri-docker-confd:
    name: Build centauri-docker-confd
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: centauri-docker-confd
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  cocoon:
    name: Build cocoon
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: cocoon
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  dockercleanup:
    name: Build dockercleanup
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: dockercleanup
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  dsp:
    name: Build dsp
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: dsp
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  ergo:
    name: Build ergo
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: ergo
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  forgejo:
    name: Build forgejo
    needs:
        - alpine
        - base
        - git
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: forgejo
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  forgejo-runner:
    name: Build forgejo-runner
    needs:
        - alpine
        - base
        - git
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: forgejo-runner
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  githubmirror:
    name: Build githubmirror
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: githubmirror
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  golink:
    name: Build golink
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: golink
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  goplum:
    name: Build goplum
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: goplum
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  httpredirect:
    name: Build httpredirect
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: httpredirect
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  identd:
    name: Build identd
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: identd
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-bot:
    name: Build irc-bot
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-bot
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-distribution:
    name: Build irc-distribution
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-distribution
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-github:
    name: Build irc-github
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-github
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-goplum:
    name: Build irc-goplum
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-goplum
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-notifier:
    name: Build irc-notifier
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-notifier
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  irc-webhook:
    name: Build irc-webhook
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: irc-webhook
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  linx-server:
    name: Build linx-server
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: linx-server
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  miniflux:
    name: Build miniflux
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: miniflux
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  pdsps:
    name: Build pdsps
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: pdsps
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  postgres-18:
    name: Build postgres-18
    needs:
        - alpine
        - base
    runs-on: alpine
    env:
      PROJECT_NAME: postgres-18
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  purser:
    name: Build purser
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: purser
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  puzzles:
    name: Build puzzles
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: puzzles
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  redis:
    name: Build redis
    needs:
        - alpine
        - base
    runs-on: alpine
    env:
      PROJECT_NAME: redis
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  registryauth:
    name: Build registryauth
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: registryauth
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  soju:
    name: Build soju
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: soju
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  sws:
    name: Build sws
    needs:
        - alpine
        - base
        - rust
    runs-on: alpine
    env:
      PROJECT_NAME: sws
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  tailscale:
    name: Build tailscale
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: tailscale
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  thp:
    name: Build thp
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: thp
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  tsp:
    name: Build tsp
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: tsp
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

  webhooked:
    name: Build webhooked
    needs:
        - alpine
        - base
        - golang
    runs-on: alpine
    env:
      PROJECT_NAME: webhooked
      REGISTRY: ${{ secrets.REGISTRY }}
      GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
      GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
      CONTEMPT_TOKEN: ${{ secrets.CONTEMPT_TOKEN }}
      REPOSITORY: ${{ github.repository }}
      ACTOR: ${{ github.actor }}
    steps:
      - name: Checkout source
        uses: actions/toolkit/git-checkout@master
        with:
          fetch-depth: '0'
      - name: Setup Go
        uses: actions/toolkit/go-setup@master
        with:
          go-version: 'stable'
      - name: Install latest contempt
        run: |
          go install github.com/csmith/contempt/cmd/contempt@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Login to private registry
        uses: actions/toolkit/docker-login@master
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASS }}
      - name: Configure Git
        run: |
          command -v git >/dev/null 2>&1 || apk add --no-cache git
          git config user.name "$GIT_USERNAME"
          git config user.email "$GIT_EMAIL"
      - name: Update
        env:
          BUILDAH_ISOLATION: chroot
          SOURCE_LINK: https://git.mouse-lake.ts.net/greboid/dockerfiles/blob/master/
        run: contempt -template Containerfile.gotpl -output Containerfile --commit --build --push --project "$PROJECT_NAME" . .
      - name: Check for tests
        id: check_tests
        run: |
          if [ -f "$PROJECT_NAME/test.sh" ]; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run integration tests
        if: steps.check_tests.outputs.has_tests == 'true'
        run: |
          command -v podman >/dev/null 2>&1 || apk add --no-cache podman
          chmod +x "$PROJECT_NAME/test.sh"
          cd "$PROJECT_NAME"
          ./test.sh "$REGISTRY/$PROJECT_NAME"
      - name: Check mirror
        id: check_mirror
        run: |
          if [ -f "$PROJECT_NAME/MIRROR" ]; then
            if [ -n "$CONTEMPT_TOKEN" ]; then
              echo "files_exists=true" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::CONTEMPT_TOKEN secret is not set — skipping ghcr.io mirror for $PROJECT_NAME"
            fi
          fi
      - name: Mirror the image
        if: steps.check_mirror.outputs.files_exists == 'true'
        run: |
          buildah login -u "$ACTOR" -p "$CONTEMPT_TOKEN" ghcr.io
          buildah tag "$REGISTRY/$PROJECT_NAME" "ghcr.io/$REPOSITORY/$PROJECT_NAME"
          buildah push "ghcr.io/$REPOSITORY/$PROJECT_NAME"
      - name: Push changes
        uses: actions/toolkit/git-try-push@master
        with:
          audience: u:1:5759a8a9-9323-4319-9847-028d8698373f

