# Generated from https://github.com/greboid/dockerfiles/blob/master/baseroot/Containerfile.gotpl
# BOM: {"apk:acl-libs":"2.3.2-r1","apk:ca-certificates-bundle":"20250911-r0","apk:libxxhash":"0.8.3-r0","apk:lz4-libs":"1.10.0-r0","apk:musl":"1.2.5-r10","apk:popt":"1.19-r4","apk:rsync":"3.4.1-r1","apk:tzdata":"2025b-r0","apk:zlib":"1.3.1-r2","apk:zstd-libs":"1.5.7-r0","image:alpine":"eb9b3154e4465f37f1bb0399eecd1174df661731937c7c32cd41a5746da7b97d"}

FROM reg.g5d.dev/alpine@sha256:eb9b3154e4465f37f1bb0399eecd1174df661731937c7c32cd41a5746da7b97d AS build

# Install packages
RUN set -eux; \
    apk add --no-cache \
        acl-libs=2.3.2-r1 \
        libxxhash=0.8.3-r0 \
        lz4-libs=1.10.0-r0 \
        musl=1.2.5-r10 \
        popt=1.19-r4 \
        rsync=3.4.1-r1 \
        zlib=1.3.1-r2 \
        zstd-libs=1.5.7-r0 \
    ;

# Install packages into rootfs
RUN \
    apk add --no-cache ca-certificates-bundle=20250911-r0; \
    apk info -qL ca-certificates-bundle | rsync -aq --files-from=- / /rootfs/; \
    apk add --no-cache musl=1.2.5-r10; \
    apk info -qL musl | rsync -aq --files-from=- / /rootfs/; \
    apk add --no-cache tzdata=2025b-r0; \
    apk info -qL tzdata | rsync -aq --files-from=- / /rootfs/; \
    true

# Create mount points and directories
# Create directories
RUN mkdir -p /rootfs/tmp /rootfs/proc /rootfs/dev /rootfs/sys /rootfs/etc; \
    chmod 01777 /rootfs/tmp

# Set up user accounts
# Set up users and groups
RUN mkdir -p /rootfs/etc; \
    echo "root:x:0:" >> /rootfs/etc/group; \
    echo "nobody:x:65534:" >> /rootfs/etc/group; \
    echo "nonroot:x:65532:" >> /rootfs/etc/group; \
    echo "root:x:0:0:root:/root:/sbin/nologin" >> /rootfs/etc/passwd; \
    mkdir -p /rootfs/root; \
    chown 0:0 /rootfs/root; \
    echo "nobody:x:65534:65534:nobody:/nonexistent:/sbin/nologin" >> /rootfs/etc/passwd; \
    echo "nonroot:x:65532:65532:nonroot:/home/nonroot:/sbin/nologin" >> /rootfs/etc/passwd; \
    mkdir -p /rootfs/home/nonroot; \
    chown 65532:65532 /rootfs/home/nonroot

# Add git config
# Copy gitconfig to /rootfs/etc/gitconfig
COPY gitconfig /rootfs/etc/gitconfig


FROM scratch

ENV SSL_CERT_FILE="/etc/ssl/certs/ca-certificates.crt"

# Copy rootfs
COPY --from=build /rootfs/ /


