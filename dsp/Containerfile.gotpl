{{- $repo := "greboid/dsp" -}}
{{- $binary := "dsp" -}}
FROM {{image "golang"}} AS build
{{ checkout_tag $repo }}
{{ go_build $repo $binary "cmd" (list) (list) (list) }}
{{ from_base_add_binary $binary (list) (list) }}
